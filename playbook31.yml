---
- name: Configrue apache2/https based on OS flavour using Error Handling
  hosts: all
  tasks:
    - block: 
        - name: Install apache2 on Ubuntu servers using apt
          apt:
            name: apache2
            state: present
            update_cache: yes
      rescue:
        - name: Install httpd on Redhat servers
          yum:
            name: httpd
            state: present
            update_cache: yes
    - name: Edit the index.html file
      copy:
        content: "IntelliQ"
        dest: /var/www/html/index.html
    - block:
        - name: Restart apache2 on ubuntu servers
          service:
            name: apache2
            state: restarted
      rescue:
        - name: Restart httpd on redhat servers
          service:
            name: httpd
            state: restarted
    - name: Check apache2/httpd response on all the servers
      uri:
        url: "{{item}}"
        status_code: 200
      with_items:
        - http://172.31.20.164
        - http://172.31.24.103
        - http://172.31.16.245
...




